server:
   port: 8888
#  port: 9090 обычно используется для конфигурации порт 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/CentralRepository
#         uri: https://github.com/name_repository/name_config
#        native:
#          search-locations: file://${user.home}/CentralRepository/
#  profiles:
#    active: native      
# профили
# ---
# spring:
#   profiles: local
#   cloud:
#     config:
#       server:
#         git:
#           uri: file:///${GIT_REPO}               
