eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    serviceUrl:
      defaultZone: http://localhost:9091/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 0   
server:
  port: 9091
spring:
  application:
    name: GrinnHrmSalaryDiscoveryServer

  