spring:
  application:
    name: SlrfactorySearch
  cloud:
    config:
      uri: http://localhost:9090
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:9091/eureka/
  instance:
    hostname: localhost
server:
  port: 8080
security:
  basic:
    enabled: false
management:
  security:
    enabled: false
    

            