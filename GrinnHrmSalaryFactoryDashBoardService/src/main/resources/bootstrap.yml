eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9091/eureka
management:
  security:
    enabled: false
security:
  basic:
    enable: false
server:
  port: 8081
spring:
  application:
    name: FactoryDashBoard
  cloud:
    config:
      uri: http://localhost:9090
